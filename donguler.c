#include <stdio.h>


int main() {

    // ---------------------------------------------------------------------------------------

    // 1'den N'e kadar olan sayıların yazdırılması
 
    // int N;

    // printf("Kaca kadar yazilsin?: ");
    // scanf("%d", &N);

    // for (int sayac = 1; sayac <= N; sayac++) {
    //     printf("%d\n", sayac);
    // }

    // ---------------------------------------------------------------------------------------

    // Faktöriyel hesabı
    // int Number;
    
    // int sonuc = 1;

    // printf("Faktoriyeli alinacak sayiyi giriniz: ");
    // scanf("%d", &Number);

    // if (Number > 0) {
    //     for (int i = 1; i <= Number; i++) {
    //         sonuc = sonuc * i;
    //     }

    //     printf("Faktoriyel sonucu: %d", sonuc);
    // } else {
    //     printf("Pozitif bir sayi giriniz");
    // }

    // ---------------------------------------------------------------------------------------

    // int sayi = 5;

    // while (sayi > 0) {
    //     printf("Sayi: %d\n", sayi);

    //     sayi = sayi - 1; // sayi--;
    // }
    
    // ---------------------------------------------------------------------------------------

    // int toplam = 0;
    // int sayi;
    // int sayac = 0;
    
    // do{
    //     printf("Bir sayi giriniz:");
    //     scanf("%d",&sayi);
    //     toplam += sayi;     // toplam = toplam + sayi
    //     sayac++;
    // }
    // while(sayi != 0);
    
    // printf("Girdiginiz %d adet sayinin toplami  %d dir", sayac, toplam);

    // ---------------------------------------------------------------------------------------



    return 0;
}