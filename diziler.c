#include <stdio.h>


int main() {

    // ---------------------------------------------------------------------------------------

    // int notlar[5] = {15, 22, 63, 44, 7};

    // printf("sayilar: %d %d %d %d %d", notlar[0], notlar[1], notlar[2], notlar[3], notlar[4]);

    // ---------------------------------------------------------------------------------------

    // dizinin eleman sayısını bulan program

    // int notlar[5] = {15, 22, 63, 44, 7 };

    // int sayi = sizeof(notlar) / sizeof(notlar[0]);

    // printf("Eleman Sayisi: %d", sayi);

    // ---------------------------------------------------------------------------------------

    // kullanıcıdan N tane sayı alıp onları bir dizide depolayan program

    // int N;
    // printf("Kac sayi gireceksiniz: ");
    // scanf("%d", &N);

    // int sayilar[N];

    // for (int i = 0; i < N; i++) {
    //     printf("Dizinin %d. elemanini giriniz: ", i);
    //     scanf("%d", &sayilar[i]);
    // }

    // printf("-----------------------------------------------------------\n");

    // for (int i = 0; i < N; i++) {
    //     printf("Dizinin %d. elemani: %d\n", i, sayilar[i]);
    // }

    // ---------------------------------------------------------------------------------------

    // int dizi[3][2] = { {7,8}, {9, 5}, {6,11} };

    // for (int satir = 0; satir < 3; satir++) {
    //     for (int sutun = 0; sutun < 2; sutun++) {
    //         printf("dizi[%d][%d] elemaninin degeri: %d\n", satir, sutun, dizi[satir][sutun]);
    //     }
    // }

    // ---------------------------------------------------------------------------------------




    return 0;
}
